#!/bin/bash
# .secrets 템플릿 파일
# 이 파일은 환경변수 템플릿입니다. 실제 사용을 위해서는:
# 1. 이 파일을 .secrets.local로 복사: cp config/.secrets config/.secrets.local
# 2. .secrets.local 파일에서 실제 값을 입력하세요
# 3. .secrets.local 파일은 Git에 추적되지 않으므로 안전합니다

# git rm --cached config/.secrets
# git ls-files --cached | grep .secrets    # 추적 중이면 결과 나옴

# Cloud (AWS, GCP, Azure)
# export OPENAI_API_KEY=your_openai_api_key_here
# export HF_API_KEY=your_huggingface_api_key_here

# Database
# export DATABASE_URL=your_database_url_here

# Other API Keys
# export GITHUB_TOKEN=your_github_token_here
# export SLACK_TOKEN=your_slack_token_here
